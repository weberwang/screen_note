# screen_note

一个按功能 DDD 分层的 Flutter 本地优先事项应用基线工程。

- 根壳层：`go_router + Riverpod + hooks` 统一承接导航、主题和国际化。
- 核心模块：`app_shell`、`task_flow`、`history_center`、`widget_bridge`、`settings_center`。
- 基础能力：`drift`、`shared_preferences`、`flutter_secure_storage`、`home_widget`、`flutter_local_notifications`。
